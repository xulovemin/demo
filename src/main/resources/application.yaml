server:
  port: 80
magic-api:
  web: /magic
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/magic?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF8&zeroDateTimeBehavior=convertToNull&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    baseline-on-migrate: true